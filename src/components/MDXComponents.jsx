// Map custom HTML elements to React components
// This makes these components available in all MDX files without imports
export const components = {
  // No custom components currently needed
};

export default components;
